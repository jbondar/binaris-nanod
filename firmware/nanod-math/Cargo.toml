[package]
name = "nanod-math"
version = "0.1.0"
edition = "2021"
description = "Pure math modules for NanoD haptic firmware (PID, FOC, haptic state)"

[dependencies]
heapless = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
